add_bib.py is a python program for adding entries to .bib files. It can grab relevant information from ISBN numbers, and has Git integration.

For information of a specific entry type or database field, type "help" followed by its name. For a list of entry types, type "help entry types". For a list of database fields type "entry database fields".